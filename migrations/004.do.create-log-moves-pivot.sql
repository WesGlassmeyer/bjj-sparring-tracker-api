CREATE TABLE log_moves_pivot (
    
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    log_entry_id INTEGER REFERENCES log_entry(id) ON DELETE CASCADE,
    move_id INTEGER REFERENCES moves(id) ON DELETE CASCADE
  
);